option(CORE_STATIC "Build the core library as a static library" ON)
option(CORE_SHARED "Build the core library as a shared library" OFF)

message("---------------------------------")
message("-- C++ Codebase Core Options --")
message(STATUS "CORE_STATIC: ${CORE_STATIC}")
message(STATUS "CORE_SHARED: ${CORE_SHARED}")
message("---------------------------------")